{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(gdb) Qsequence Debug",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/Qsequence",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "setupCommands": [
                { "description": "Enable pretty-printing", "text": "-enable-pretty-printing", "ignoreFailures": true }
            ],
            "preLaunchTask": "build Qsequence"
        }
    ]
}
